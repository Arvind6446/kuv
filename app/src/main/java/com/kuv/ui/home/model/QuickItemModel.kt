package com.kuv.ui.home.model

data class QuickItemModel(
    // on below line we are creating a
    // two variable one for course name
    // and other for course image.
    var title: String,
    var image: Int,
    var id:Int
)