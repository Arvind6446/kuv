<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Kuv"
        tools:targetApi="31">
        <activity
            android:name=".app.android.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".app.android.SignUpActivity"/>
        <activity android:name=".MainActivity"/>
        <activity android:name=".app.android.OtpActivity"/>
        <activity android:name=".app.android.LanguageActivity"/>
        <activity android:name=".app.android.ConnectBlue"/>
        <activity android:name=".RecordActivity"/>
        <activity android:name=".FuelActivity"/>
        <activity android:name=".DashHomeActivity"/>
        <activity android:name=".AddCarForm"/>
        <activity android:name=".AddCarLayout"/>
        <activity android:name=".RecordFormLayout"/>
        <activity android:name=".DiagnosticLayout"/>

    </application>

</manifest>